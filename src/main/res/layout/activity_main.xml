<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0288D1"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Meal input field with proper accessibility and sizing -->
    <EditText
        android:id="@+id/mealInput"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginHorizontal="24dp"
        android:autofillHints=""
        android:contentDescription="@string/desc_meal_input"
        android:hint="@string/hint_meal_input"
        android:inputType="textCapWords"
        android:padding="12dp"
        android:textColor="@android:color/black"
        android:textColorHint="@android:color/darker_gray"
        tools:ignore="ContentDescription" />

    <!-- Button with accessible label -->
    <Button
        android:id="@+id/suggestButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:contentDescription="@string/desc_button_suggest"
        android:text="@string/button_suggest" />

    <!-- Result text output -->
    <TextView
        android:id="@+id/resultText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:textColor="@android:color/black"
        android:textSize="18sp" />

    <ImageView
        android:id="@+id/imageView2"
        android:layout_width="259dp"
        android:layout_height="300dp"
        app:srcCompat="@drawable/food_pyramid_healthy_eating_fruits_vegetables_fruit_collage_white_background_96478890" />
</LinearLayout>


